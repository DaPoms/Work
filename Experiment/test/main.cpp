#include <iostream>
#include <ctime>
#include <string>
#include <algorithm>
int main()
{
    if (0) // 0 always = false
        {
            std::cout << "Hello world";
        }
     if (5) // any nonzero number always = true
        {
            std::cout << "Hello";
        }
}
