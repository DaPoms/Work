Project 3 was an extension of project 2, in which instead of using a class object provided by the teacher (with no way to view the code for it), we had to recreate it ourselves so that the
runner file (the file with main in it) in project 2 works the same way (or better). 
