public class Unicode
{
    public static void main()
    { // couldn't get it working :C
        int num = 0x1F60;
        String emoji = new String(Character.toChars(num));
        System.out.println(emoji);

    }
}
